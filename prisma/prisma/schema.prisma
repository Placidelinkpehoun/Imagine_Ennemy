generator client {
  provider = "prisma-client-js"
}

model Specificity {
  id        String   @id @default(cuid())
  text      String
  posX      Float?
  posY      Float?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  attributes SpecificityAttribute[]

  @@map("specificities")
}

model SpecificityAttribute {
  id            String   @id @default(cuid())
  specificityId String
  entityId      String
  attributeId   String
  createdAt     DateTime @default(now())

  specificity Specificity @relation(fields: [specificityId], references: [id], onDelete: Cascade)
  entity      Entity      @relation(fields: [entityId], references: [id], onDelete: Cascade)
  attribute   Attribute   @relation(fields: [attributeId], references: [id], onDelete: Cascade)

  @@unique([specificityId, entityId, attributeId])
  @@map("specificity_attributes")
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Attribute {
  id          String   @id @default(cuid())
  name        String   @unique
  description String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  gameClasses  GameClassAttribute[]
  entities     EntityAttribute[]
  specificities SpecificityAttribute[]

  @@map("attributes")
}

model GameClass {
  id          String   @id @default(cuid())
  name        String
  description String?
  color       String
  parentId    String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  parent     GameClass?             @relation("GameClassHierarchy", fields: [parentId], references: [id])
  children   GameClass[]            @relation("GameClassHierarchy")
  attributes GameClassAttribute[]

  @@map("game_classes")
}

model Entity {
  id          String   @id @default(cuid())
  name        String
  description String?
  positionX   Float?
  positionY   Float?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  attributes EntityAttribute[]
  connections Connection[] @relation("EntityConnections")
  targets    Connection[] @relation("EntityTargets")
  specificities SpecificityAttribute[]

  @@map("entities")
}

model Connection {
  id        String   @id @default(cuid())
  sourceId  String
  targetId  String
  type      String
  createdAt DateTime @default(now())

  source Entity @relation("EntityConnections", fields: [sourceId], references: [id], onDelete: Cascade)
  target Entity @relation("EntityTargets", fields: [targetId], references: [id], onDelete: Cascade)

  @@map("connections")
}

model GameClassAttribute {
  gameClassId String
  attributeId String
  createdAt   DateTime @default(now())

  gameClass GameClass @relation(fields: [gameClassId], references: [id], onDelete: Cascade)
  attribute Attribute @relation(fields: [attributeId], references: [id], onDelete: Cascade)

  @@id([gameClassId, attributeId])
  @@map("game_class_attributes")
}

model EntityAttribute {
  entityId    String
  attributeId String
  createdAt   DateTime @default(now())

  entity    Entity    @relation(fields: [entityId], references: [id], onDelete: Cascade)
  attribute Attribute @relation(fields: [attributeId], references: [id], onDelete: Cascade)

  @@id([entityId, attributeId])
  @@map("entity_attributes")
}